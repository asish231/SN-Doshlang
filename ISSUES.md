# Open Issues

Resolved work lives in `RESOLVED_ISSUES.md`.

## Current Status

- No known blocking failures in `tests/runtime_test.sh`; contract/follows + `spawn` case uses blueprint id from `object_blueprint_ids` for method dispatch (see `RESOLVED_ISSUES.md`).
- Ongoing language work continues when the baseline is green.
